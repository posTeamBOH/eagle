<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "mybatis-3-mapper.dtd">
  
  
<mapper namespace="com.choice.eagle.dao.CuisineDao">

	<!-- 测试完成 -->
   <select id="selectAllCuisines" resultType="Cuisine" >
   		select * from sys_cuisine
   </select>
   
   <select id="selectCuisineId" resultType="String">
   		select cuisine_id from sys_cuisine where cuisine_name = #{cuisineName}
   </select>
 
</mapper>