<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.choice.eagle.dao.RorderDao">

   <select id="selectMenuByOrderId" parameterType="String" resultType="com.choice.eagle.entity.Menu">
  	SELECT menu_id, COUNT(*) FROM sys_rorder 
  	WHERE rorder_id = #{rorderId} 
  	GROUP BY menu_id 
  </select>
  
   <update id="updateMenuStatus" parameterType="String">
  	 update sys_rorder
  	 set rorder_type = '1'
  	 where rorder_id = #{rorderId}
  </update>
</mapper>